# vnx-ai-netconfig
Automated network configuration system for VNX virtual scenarios using generative AI. An orchestrator queries an LLM API, generates Linux ifupdown configurations for each node, distributes them via SCP and validates end-to-end connectivity. Bachelor's thesis — ETSIT UPM
